<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Question Page | AudioAdvisor</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body>

    <h1>Student Data</h1>
    <form id="chooseName" method="POST" action="/studentData">
        <input type="text" name="name" id="name" placeholder="Student Name">
        <button>Request</button>
    </form>
    <label>
        <%= student.name %>
            <%= student.computerNumber %>
    </label>
    <% if (student.note.length===0) { %>
        <p>There are no notes for this student.</p>
        <% } else { %>
            <ul>
                <% for( const note of student.note ) { %>
                    <li>
                        <p>
                            <%= note.noteText %>
                        </p>
                    </li>
                    <% } %>
            </ul>
            <% } %>

</body>